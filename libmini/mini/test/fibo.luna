# calculates the nth fibonacci number (iteratively)

main(par fibo)
   {
   var i;

   var a,b,c;

   a=1;
   b=1;

   for (i=1, i<fibo, i++)
      {
      c=a+b;
      a=b;
      b=c;
      }

   return(b);
   }
